# LLM API Key (required)
ANTHROPIC_API_KEY=your-api-key-here

# Database (required for Prisma)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/coding_agent

# Agent connects to server (required when server port differs from 3000)
AGENT_SERVER_URL=http://localhost:3000

# Local agent file sandbox (gitignored). The agent package `npm run dev` uses ../../.agent-workspace
# from packages/agent. Override when invoking the CLI manually: --working-dir /path/to/sandbox

# Shared workspace directory for session file downloads (and agent writes when set).
# When set, the agent uses this as its base directory so it matches the server's zip path.
# In Docker Compose, server and agent both set WORKSPACE_DIR=/workspace and mount the workspace volume.
# WORKSPACE_DIR=/workspace

# Host port mappings (change if defaults conflict)
DB_PORT=5432
SERVER_PORT=3000
WEB_PORT=5173

# Web UI: collapse long fenced code / tool panels after this many lines (optional; default 20)
# VITE_CODE_BLOCK_MAX_LINES=20
